# 复制为 .env 后按需修改，全部可留空使用默认值

# ── 版本 ────────────────────────────────────────────────────────────────────
# 当前使用的版本号。这是宿主机侧唯一的版本变量：
#   · 镜像 tag 默认跟随它 → 拉 baey666/music-monitor:v<APP_VERSION>
#   · 需要回滚时，把它改成旧版本号（如 1.0.0）再 pull 即可
#
# ⚠️ 不要手改数字 —— 用 scripts/version.sh bump 统一改，它会同步代码里的 __version__、
#    Dockerfile、CHANGELOG 并打 git tag，避免「代码版本和镜像版本对不上」。
#    校验是否一致： ./scripts/version.sh check
APP_VERSION=1.0.0

# ── 镜像地址（可选，一般不用改）──────────────────────────────────────────────
# 留空 = 自动使用 baey666/music-monitor:v<APP_VERSION>（推荐，改版本号就自动跟随）。
# 需要换仓库、或坚持固定用 latest 时，取消下面这行注释并改成你的地址；
# 一旦设置，就会覆盖上面的版本派生规则。
#
# 其它仓库示例（第一段是命名空间 / 你的 Docker ID）：
#   阿里云 ACR     registry.cn-hangzhou.aliyuncs.com/yournamespace/music-monitor:v1.0.0
#   腾讯云 TCR     ccr.ccs.tencentyun.com/yournamespace/music-monitor:v1.0.0
#   私有 registry  192.168.1.10:5000/music-monitor:v1.0.0
#   本地构建        music-monitor:latest    （配合 docker compose up -d --build）
#
# ⚠️ 国内直连 Docker Hub 常超时，拉取需要配镜像加速，见 docs/deploy.md。
# MONITOR_IMAGE=baey666/music-monitor:latest

# ── 构建期源加速（国内构建强烈建议打开）────────────────────────────────────
# ⚠️ 这里只管「构建过程」用的源，和拉基础镜像不是一回事：
#    · 拉 python:3.12-slim / guohuiyuan/go-music-dl 走 Docker 守护进程的 registry-mirrors
#      （Docker Hub 在国内经常直接超时，必须配，见 docs/deploy.md「国内网络注意事项」）
#    · 下面两项管的是构建容器内部的 apt 和 pip
#
# APT_MIRROR  替换 Debian 软件源，留空用官方源。常用：mirrors.aliyun.com
# PIP_INDEX   替换 PyPI 源，留空用官方源。常用：https://pypi.tuna.tsinghua.edu.cn/simple
#
# 在国内 NAS 上就地构建时，把下面两行填上：
APT_MIRROR=
PIP_INDEX=

# ── 配置与数据落盘位置（宿主机路径）────────────────────────────────────────
# 卷挂载的语法是「宿主机路径 : 容器内路径」，在 docker-compose.yml 里。
# ★ 改左边的宿主机路径，**不要改右边的容器内路径**——那是程序内部写死的。
#
# 分工原则：**配置归 config/，大文件归 data/**。
# 好处：备份只要打包 config/ 目录（几 MB），不用碰几百 GB 的音乐。
#
# ┌ ENGINE_CONFIG_DIR ─ 容器内 /home/appuser/data
# │   引擎的全部设置与登录态：
# │     settings.db            引擎设置（含 downloadDir 下载目录这一项）
# │     settings.db-wal / -shm  SQLite 日志与共享内存（必须与 db 同目录）
# │     cookies.json           各平台登录态（决定能拿到什么音质）
ENGINE_CONFIG_DIR=./config/engine
#
# ┌ MONITOR_CONFIG_DIR ─ 容器内 /app/data
# │   监控服务自己的配置库 monitor.db（监控项、曲目记录、运行历史）
MONITOR_CONFIG_DIR=./config/monitor
#
# ┌ DOWNLOADS_DIR ─ 容器内 /home/appuser/data/downloads
# │   **音乐文件**。属于大文件而非配置，单独放 data/。
# │   想放到第二块盘就直接换成绝对路径，例如 /vol2/music
DOWNLOADS_DIR=./data/downloads
#
# 迁移提醒：这三个变量取代了旧版的 ENGINE_DATA_DIR / MONITOR_DATA_DIR。
# ⚠️ 不要把 MONITOR_DATA_DIR 写进 .env —— 这个名字容易和「容器内路径」混淆，
#    服务内部统一用 MONITOR_DB_DIR（容器内，默认 /app/data），两者别混用。
# 若你之前已经部署过，改完记得把老数据搬过来（见 README「从旧版 data/ 布局迁移」）。

# ── 端口与行为 ──────────────────────────────────────────────────────────────
# 宿主机映射端口
ENGINE_PORT=8085          # 上游 go-music-dl 的 Web 界面端口
MONITOR_PORT=9090         # 本项目监控控制台端口

# 引擎路由前缀（官方默认 /music）
ENGINE_PREFIX=/music

# 调度器心跳（秒）：越大越省资源，越小发现新歌越快
TICK_SECONDS=60

# 并发下载数
DOWNLOAD_CONCURRENCY=3

# 默认目标音质：standard | high | lossless | hires
#   standard  ≈ 128kbps
#   high      ≈ 320kbps
#   lossless  ≈ FLAC 无损（需要对应平台会员 Cookie，否则会降级）
#   hires     ≈ Hi-Res 24bit（绝大多数情况无法满足，会自动降级）
DEFAULT_QUALITY=lossless

# 时区
TZ=Asia/Shanghai
